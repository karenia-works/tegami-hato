.container {
    display: flex;
    min-height: 45vh;
}

.welcome {
    margin: auto;
    display: flex;
    flex-direction: column;
    align-items: flex-start;

    .title {
        margin: 0;
    }

    .info {
        margin-top: var(--theme-pad-01);  
        margin-bottom: var(--theme-pad-04);  
    }
}

app-searchbar {
    width: 50vw;
    min-width: 205px;
    max-width: 450px;
}

.pic_contain {
    z-index: 0;
    position: absolute;
    bottom: 0px;
    right: 0px;

    .backgroundpic {
    }
}